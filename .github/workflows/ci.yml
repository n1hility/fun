name: Dmesg

on: [push, pull_request]

jobs:
  other-job1:
    name: Crazy  Job
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: |
        sudo dmesg -w -T &
        tail /dev/zero
        echo end
